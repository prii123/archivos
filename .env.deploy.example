# Deployment Configuration
# Copy this file to .env.deploy and fill with your server details
# Usage: source .env.deploy && ./deploy.sh

# Required: Your production server IP or domain
DEPLOY_HOST=your-server-ip-or-domain.com

# Optional: SSH user (default: root)
DEPLOY_USER=root

# Optional: Remote directory (default: /opt/docmanager-drive)
DEPLOY_DIR=/opt/docmanager-drive

# Example for DigitalOcean:
# DEPLOY_HOST=157.230.45.123
# DEPLOY_USER=root
# DEPLOY_DIR=/opt/docmanager-drive
